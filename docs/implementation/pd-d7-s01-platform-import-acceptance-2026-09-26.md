# PD-D7-S01-PF acceptance record — 2026-09-26

**Status:** Terminal for the disposable WP-PD7PF-06 handoff. Consumer writer and authentication cutover remain local and are owned by D7-S02.

## Exact handoff

- Platform source commit before the rehearsal: `297df6c4a9f67f8b1c90d29087212ca44e6be8c9`.
- CTRL source candidate: `5c1f7a1bd06b0077088ad6e02eedefa1618ee24c`.
- IMS source candidate: `5de0136b81e72111214fe860b7b0ea4db8cbe8a8`.
- CTRL source database SHA-256: `880537aa812e20b20694c4f84e88efcdbdfaaff8c3d64e6ded01730a143f421e0`.
- IMS source database SHA-256: `1b2919a1e092e3d82505db6846609f4d6f4c54823bdbe47d9a6da7751029443b`.
- Platform restore point: `D7-S01-PF-RESTORE-2026-09-24`.
- Restore-point SHA-256: `a4d931d8dca81236c8b67eb0e46d5f0bcb4c956833af838a8b4e76799eca1dd5`.

## Explicit allocation decision

The user-authorized disposable rehearsal used `d7-s01-disposable-source-isolated-allocation-v1`:

1. A source record with no approved canonical key receives a fresh Platform ID generated by the Platform ID shape. The ID is recorded only in the signed manifest and is never calculated from a local integer or source ID.
2. CTRL and IMS records remain separate unless all signed user identity fields agree exactly. The two `admin@example.com` / `Administrator` / active records were merged into one canonical user because the signed source fields are identical. No organisation or workspace was merged.
3. Source IDs remain provenance references. They do not become Platform IDs and do not decide cross-product identity.

The signed report digest is `c5b3a5db9e2866c5fa20bdfa1fc3bade4bfa371cadc130ba8fae57b9ea3f453f`. It contains 22 source records and 21 approved manifest proposals with zero blocked, ambiguous or collision rows.

## Operator authorization

- Approval ID: `D7-S01-AUTH-2026-09-26`.
- Operator ID: `operator.tockrplatform.rehearsal`.
- Key ID: `d7-s01-rehearsal-ed25519-20260926`.
- Scope: `production` in the versioned import envelope, constrained by the signed reason to a disposable restored database; no production cutover or writer activation.
- Manifest ID: `2b831b31cd5686ef91c4ac66824f1f1301254d26876ad7cfcfc0e56084f3d211`.
- Manifest SHA-256: `9c2f8adbd7831025137907dcb500626cfa438636afc0f5bd0e07ba4a5de78079`.

Private rehearsal key material and raw inventories remain outside the repository in the disposable evidence directory.

## Execution evidence

The protected importer was run against a writable copy of the restore point:

| Gate | Receipt | Result |
| --- | --- | --- |
| Dry run | 21 records | PASS; signed manifest, source snapshots and execution authorization verified |
| Bounded apply | `next_index=7` | PASS; 7 records committed and checkpointed |
| Resume | `next_index=21` | PASS; completed with 21 created records |
| Replay | `status=idempotent` | PASS; 21 records skipped and no duplicate canonical rows created |
| Rollback | `status=rolled_back` | PASS; 21 created records compensated |

After rollback, canonical users, organisations, workspaces and memberships were zero; 21 import records, 21 compensation records, 21 outbox events and 16 audit events remained. `PRAGMA foreign_key_check` returned no rows. Migration version 14 preserves the actor public ID in audit history while allowing imported user rows to be compensated.

## Consumer convergence

CTRL and IMS each used the signed manifest and completed receipt, prepared only its own source mappings, installed its source-scoped snapshot, applied `cur_22`, replayed the same event, and finalized only after a current, gap-free projection.

- CTRL: 17 source/projection records, 7 mappings, `current`, cursor `cur_22`, zero gaps and zero pending events, source refs matched, canonical state matched, product fingerprint unchanged, runtime modes remained local.
- IMS: 5 source/projection records, 3 mappings, `current`, cursor `cur_22`, zero gaps and zero pending events, 5 source refs/mappings/projections verified, product fingerprint unchanged, runtime modes remained local.

The consumer post-import verifiers now scope projection checks to the consumer's signed source references. This keeps the shared manifest authoritative while preventing a CTRL database from being required to own IMS-local rows (and vice versa).

## Validation

Platform `go test ./...`, unit, integration, architecture, security, migration, quality and race profiles passed. Linux `GOOS=linux GOARCH=amd64 go build ./...` and `GOOS=linux GOARCH=arm64 go build ./...` also passed. The Docker build profiles were then rerun through the WSL2 Docker daemon (server `29.8.1`) with `docker buildx build --platform linux/amd64 ... --load` and `docker buildx build --platform linux/arm64 ... --load`; both image builds completed successfully. CTRL and IMS `go test ./...` passed after the consumer verifier fixes; IMS also restored its required embedded `presentation.css` asset so the full test/build graph is valid.

